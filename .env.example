# Judge-01 Scenario Eval backend mode: "stub" | "sagemaker"
# Only D3N models (via SageMaker) are used in production. LLMs are never backends.
JUDGE_BACKEND_MODE=stub

# --- SageMaker mode (D3N Judge-01 scenario-eval model) ---
SAGEMAKER_ENDPOINT_NAME=judge-01-scenario-eval
AWS_DEFAULT_REGION=us-east-1
